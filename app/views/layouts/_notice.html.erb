<% if notice.present? || alert.present? %>
  <div class="w-full flex mx-auto mt-6">
    <p class="mx-auto py-2 px-3 <%= notice ? 'bg-green-50 text-green-700' : 'bg-red-50 text-red-700' %> font-medium rounded text-lg"><%= notice || alert %></p>
  </div>
<% end %>